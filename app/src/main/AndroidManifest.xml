<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.home">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity android:name=".Boundary.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Boundary.Article"/>

        <activity android:name=".Boundary.Login"/>

        <activity android:name=".Boundary.RegistrationEmail"/>

        <activity android:name=".Boundary.RegistrationPassword"/>

        <activity android:name=".Boundary.Registration"/>

        <activity android:name=".Boundary.Search"/>

        <activity android:name=".Boundary.Profile"/>

        <activity android:name=".Boundary.ProfileUpdate"/>

        <activity android:name=".Boundary.Catalog"/>

        <activity android:name=".Boundary.Data"/>

        <activity android:name=".Boundary.OrderDetails"/>

        <activity android:name=".Boundary.Menu"/>
        
        <activity android:name=".Boundary.ShoppingCart"/>
        
        <activity android:name=".Boundary.PaymentDetails"/>

        <activity android:name=".Boundary.Paypal"/>

    </application>

</manifest>